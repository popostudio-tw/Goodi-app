## 2024-05-22 - [Missing Memoization in ScoreChart]
**Learning:** Documentation/Memory claimed `ScoreChart` was optimized with `React.memo`, but the code showed it was not. This highlights that documentation can drift from code reality.
**Action:** Always verify "known" optimizations in code before assuming they exist.
